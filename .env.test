# define your env variables for the test env here
KERNEL_CLASS='App\Kernel'
APP_SECRET='$ecretf0rt3st'
# Fixed value so the kernel boots; the functional tests send this as the bearer
# token. Not a secret -- the test env never faces a network.
PUBLISH_API_TOKEN='test-api-token-0123456789'

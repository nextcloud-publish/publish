# Deployment

A sub-page: a folder whose content is this `Readme.md`. It renders to
`Deployment/index.html`, and its own leaf pages render one level below it.

Its presence in the fixture is what makes the build exercise `SiteBuilder`'s
recursion rather than just the flat top level.
